# V22 results — magnitude products

Generated by `validation/report_v22.py`. Bars were frozen in `VALIDATION_V22.md` before any benchmark cell was run.

## H-V22.2 — the Apnea veto

Read first because it is a veto, not a contribution. V11 fell to 0.478 AUC here, below chance, and did it silently.

| arm | AUC | margin vs baseline | verdict |
|---|---|---|---|
| `rocket_static` (baseline) | 0.841 | — | — |
| `prod_split` | 0.831 | **-1.0** | ok |
| `prod_margin` | 0.835 | **-0.7** | ok |
| `prod_both` | 0.845 | **+0.4** | ok |

**PASS — no arm collapses on Apnea.** The argument in `VALIDATION_V22.md` §2 holds: these products feed trees, and a tree returns a leaf value however large its input, so V11's linear extrapolation failure has no route here.

## H-V22.1 — does the diagnostic improve?

`amp_regression`, 5 seeds, RMSE. Bar is a **4%** reduction — set below the **8.4%** an oracle product column reaches (§2), because a bar above a measured ceiling is unpassable and a bar at it is a demand for luck.

| arm | RMSE | reduction | verdict |
|---|---|---|---|
| `rocket_static` | 0.4071 | — | — |
| `prod_split` | 0.4029 | **+1.0%** | below bar |
| `prod_margin` | 0.3953 | **+2.9%** | below bar |
| `prod_both` | 0.3970 | **+2.5%** | below bar |

**FAIL** — best arm `prod_margin` at +2.9% against a 4% bar (35% of the oracle ceiling).

## H-V22.3 — does anything else regress?

No scenario worse than baseline by more than 2% RMSE or 1 point. `amp_regression` is one of 5 and a fix that costs the others is not a fix.

| scenario | metric | `prod_split` | `prod_margin` | `prod_both` |
|---|---|---|---|---|
| amp_regression | rmse | +1.0% | +2.9% | +2.5% |
| bump_order | roc_auc | +0.0 | +0.0 | -0.0 |
| lead_lag | roc_auc | +12.5 | +0.0 | +12.5 |
| slope_window | roc_auc | -0.0 | +0.0 | -0.0 |
| timing | roc_auc | +0.0 | -0.0 | +0.0 |

**PASS — nothing regresses beyond tolerance.** Covered 5 of 5 scenarios.

## Verdict

**Does not ship.** The diagnostic did not clear its bar, so two routes to a magnitude product both fell short under a measured 8.4% ceiling.

Per `VALIDATION_V22.md` §4 that says the operands are still wrong rather than the currency — most likely the bank holds no clean amplitude carrier for the signal window at all, which is a statement about the *bank* and sends this back to item 1 rather than forward.

`VALIDATION_V22.md` §5 named a `prod_margin`-only win as the outcome it would least like, because that arm multiplies the base's margin by a static the margin already contains. Worth checking against the table above before drawing any conclusion about the mechanism.
